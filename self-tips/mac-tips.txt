 显示桌面command+F3
最大化不全屏 option+shift+最大化
修改default shell chsh -s /bin/zsh
最小化： command + Option + M
最大化： command + control + f
锁屏： control + shift + power
pageup: fn+向上箭头
pagedown:fn+向下箭头
前往特定的文件夹：shift+command+G，可能需要在访达页面才行
隐藏当前窗口 command+H
隐藏其它窗口option + command + H
打开spotlight command + space
修改hostname等：
sudo scutil --set ComputerName "newname"
sudo scutil --set LocalHostName "newname"
sudo scutil --set HostName "newname"
terminal当前的filder在finder中打开： open .
强制退出无响应的程序： cmd+option+esc, 然后选择程序强制退出
键盘左滑、右滑： control+left, control+right
锁屏 control + command+q
退出登陆  shift + command + q




install wget
ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
brew install wget --with-libressl

install thrift-0.10.0
brew unlink thrift
brew install https://raw.githubusercontent.com/Homebrew/homebrew-core/16ebe5f1843e6cb54856311ff0f676be53007329/Formula/thrift.rb



外接键盘设置f1~f8为默认功能键

Search Results
Featured snippet from the web
If you prefer the top row of keys to always behave as standard function keys without holding the Fn key:

    Choose System Preferences from the Apple menu.
    Click Keyboard.
    Click the Keyboard tab if it's not already highlighted.
    Select "Use all F1, F2, etc. keys as standard function keys"

修改状态栏位置（程序坞）
