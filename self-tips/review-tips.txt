任何server应该有start有stop

可扩展的*define或typedefine要统一使用

featurePlugin需检查用到的取数是否都置位了
若是修改己有代码，需考虑AB的事情，即即有代码不能简单删除，可能需要增加开关。
map取数要考虑找不到的情况。使用也是如此
指针new出来要找到return的地方
慎用头文件的static，那样会每个引用头文件的地方都新建一个。要数据共享还是c文件里面定义，头文件内extern引用比较好。
lambda表达式要注意变量的生命周期，类成员变量要注意类的生命周期，需要考虑超时的情况
凡是用到指针前，都需判断，无论指针是否默认初始化了
For 遍历vector之类的大小需与待遍历内容一一对应。若多个变量不完全对应，需检查size大小一致。
if的括号都要加上



